# Frontend Client API Base URL
# In local development or Nginx reverse proxy setup, leave as '/api'
# For standalone frontend deployment (e.g. Vercel/Netlify), set to backend domain:
# VITE_API_BASE_URL=https://api.yourdomain.com/api
VITE_API_BASE_URL=/api
